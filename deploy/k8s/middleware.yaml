# LAN + Tailscale only, same guard as the sidequest internal planes. Lantern is never public.
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: lan-only
spec:
  ipAllowList:
    sourceRange:
      - 192.168.56.0/24 # home LAN
      - 100.64.0.0/10   # Tailscale
